Task 6: Create Better Auth server config (src/lib/auth.ts)
==========================================================
Date: 2026-02-17

## File Created
- src/lib/auth.ts (27 lines)

## Configuration Summary
- Database: drizzleAdapter with provider 'pg', schema namespace
- Auth: emailAndPassword enabled, NO social providers
- Session: 7-day expiry, 1-day update age, 5-minute cookie cache
- Plugin: tanstackStartCookies() as LAST (and only) plugin
- baseURL: process.env.BETTER_AUTH_URL || 'http://localhost:3000'
- secret: process.env.BETTER_AUTH_SECRET

## Verification
- TSC: 0 errors in src/lib/auth.ts (confirmed via tsc --noEmit + LSP diagnostics)
- All TSC errors are in old convex/ directory (pre-migration artifacts, out of scope)
- Biome lint suppression added for namespace import (same pattern as src/db/index.ts)

## Dependencies Used
- db from @/db (postgres-js + drizzle-orm, prepare: false for Planetscale)
- schema from @/db/schema (user, session, account, verification + app tables)
- BETTER_AUTH_SECRET validated in src/env.ts serverEnvSchema

## Blocks
- Task 8: Auth API route handler
- Task 10: Saved searches server functions
- Task 12: Auth client configuration
- Task 13: Auth middleware/guards
